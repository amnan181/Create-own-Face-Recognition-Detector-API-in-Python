# Create own Face Recognition Detector API in Python
Create own Face Recognition Detector API in python.Only some images to detect image with better accuracy.This code written in python.

# Firstly Install
	> pip install face_recognition
